<h1>Artists</h1>


<h3><%= pluralize(@artists.count, 'Artist') %></h3>
<ul>
  <% @artists.each do |a| %>
    <li><%= link_to a.name, artist_path(a) %> has <%= a.songs.count %> song(s)
    
    </li>
  <% end %>
</ul>